// User model structure
// id: string
// username: string
// email: string
// password: string (hashed)
// role: 'user' | 'moderator' | 'admin'
// profile: { avatar: string, description: string, stats: { wins, losses, draws, noContest }, rank: number, score: number }

module.exports = {};
